import { UIFrame } from "../ui";

export const C_LevelSquish = {
    ConvertFollowerLevel: (level: number, maxFollowerLevel: number): number => {
        return 0;
    },
    ConvertPlayerLevel: (level: number): number => {
        return 0;
    },
};
