import { UIFrame } from "../ui";

export const C_SocialRestrictions = {
    IsMuted: (): boolean => {
        return false;
    },
    IsSilenced: (): boolean => {
        return false;
    },
    IsSquelched: (): boolean => {
        return false;
    },
};
